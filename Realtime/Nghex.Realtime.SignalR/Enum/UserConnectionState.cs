namespace Nghex.Realtime.SignalR.Enum
{
    /// <summary>
    /// Enum define the connection state of a user
    /// </summary>
    public enum UserConnectionState
    {
        /// <summary>
        /// User is disconnected
        /// </summary>
        Disconnected = 0,
        
        /// <summary>
        /// User is connected
        /// </summary>
        Connected = 1
    }
}